# https://github.com/astral-sh/ty/blob/main/docs/reference/configuration.md
[environment]
# for human reading only, we have pixi for different libs at runtime
# extra-paths = ["./third_party/docling", "./third_party/marker"]
